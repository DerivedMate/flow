{ ~f: a.
  { + 1 a } => { + &0 1 }
            => { - &0 1 }
            => {(- &0 1; 1)}
            => { ~fact: m, n.
                  > m 0 | {(- m 1; * m n)} => ~fact
                        | n
               }
            => { < &0 50 }
            | True
            | False
} => let ~f

{[3, 6]} => map ~f => { <~ List<Bool> }